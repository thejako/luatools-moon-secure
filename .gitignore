# Build output.
dist/

# Editor / OS noise.
.DS_Store
*.swp
*~

# Runtime artefacts from testing the canonical plugin tree in place.
plugin/backend/temp_dl/
plugin/backend/data/
plugin/backend/lua_runtime.log
plugin/backend/loadedappids.txt
plugin/backend/appidlogs.txt

# Local backup of published release assets (not tracked).
.release-backup/

# Local reference / testing archives.
slsteam.zip
slsteam_dir/
